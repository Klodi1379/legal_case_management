# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database (for future PostgreSQL migration)
DB_NAME=legal_case_db
DB_USER=db_user
DB_PASSWORD=db_password
DB_HOST=localhost
DB_PORT=5432

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Security
DOCUMENT_ENCRYPTION_KEY=generate-with-cryptography-fernet

# AI Services
ENABLE_AI_FEATURES=True
DEFAULT_LLM_MODEL=gemma-3-12b-it-qat
DEFAULT_LLM_ENDPOINT=http://127.0.0.1:1234/v1/chat/completions

# Other Settings
DJANGO_SETTINGS_MODULE=legal_case_management.settings.development
